# instalar a biblioteca de ambiente virtual

`pip install virtualenv`

# habilitar o nosso ambiente

`virtualenv venv`

# abrir o ambiente virtual

`venv\Scripst\Activate`

# instalar dependencia

`pip install -r requirementes.txt`